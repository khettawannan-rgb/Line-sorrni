<!doctype html>
<html lang="th">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title || 'NILA · Admin Launcher' %></title>
  <style>
    :root {
      color-scheme: dark;
      font-family: 'IBM Plex Sans Thai', 'Prompt', 'Sarabun', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: #05070d;
      color: #f4f5f9;
    }
    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 32px 16px;
      background: radial-gradient(circle at 20% 20%, rgba(88, 132, 255, 0.35), transparent 60%), radial-gradient(circle at 80% 0%, rgba(255, 129, 217, 0.28), transparent 55%), #020409;
    }
    .card {
      max-width: 420px;
      width: 100%;
      background: rgba(9, 13, 24, 0.9);
      border: 1px solid rgba(88, 132, 255, 0.2);
      border-radius: 18px;
      padding: 28px 24px;
      text-align: center;
      box-shadow: 0 22px 50px rgba(10, 18, 43, 0.4);
      backdrop-filter: blur(18px);
    }
    h1 {
      font-size: 1.6rem;
      margin-bottom: 0.5rem;
      letter-spacing: 0.01em;
    }
    p {
      margin: 0.5rem 0 1rem;
      line-height: 1.6;
      color: rgba(230, 234, 249, 0.82);
    }
    .status {
      margin-top: 1.5rem;
      padding: 0.75rem 1rem;
      border-radius: 12px;
      background: rgba(88, 132, 255, 0.12);
      font-weight: 500;
      color: rgba(210, 220, 255, 0.9);
    }
    a.btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      margin-top: 1.25rem;
      padding: 0.75rem 1.25rem;
      border-radius: 12px;
      background: linear-gradient(135deg, rgba(88, 132, 255, 0.95), rgba(110, 89, 255, 0.95));
      color: #0f111f;
      font-weight: 600;
      text-decoration: none;
      box-shadow: 0 10px 20px rgba(88, 132, 255, 0.25);
    }
    .muted {
      font-size: 0.85rem;
      color: rgba(186, 196, 230, 0.65);
      margin-top: 1rem;
    }
  </style>
  <script src="https://static.line-scdn.net/liff/edge/2/sdk.js" defer></script>
  <script>
    const liffId = '<%- liffId %>';
    const baseUrl = '<%- baseUrl %>';
    const targetPath = '<%- targetPath %>';

    async function launchAdmin() {
      const statusEl = document.getElementById('status');
      try {
        await liff.init({ liffId });
        const destination = (baseUrl || window.location.origin) + targetPath;
        statusEl.textContent = 'กำลังเปิดหน้าแอดมินในเบราว์เซอร์ภายนอก...';
        liff.openWindow({ url: destination, external: true });
        setTimeout(() => {
          statusEl.textContent = 'หากไม่ถูกพาออก ให้กดปุ่มด้านล่างเพื่อเปิดด้วยตนเอง';
          document.getElementById('fallback-link').hidden = false;
        }, 2500);
      } catch (err) {
        console.error('[LIFF][Launch error]', err);
        statusEl.textContent = 'ไม่สามารถเปิด LIFF ได้ อนุญาตให้เปิดลิงก์ภายนอกและลองใหม่อีกครั้ง';
        document.getElementById('fallback-link').hidden = false;
      }
    }

    document.addEventListener('DOMContentLoaded', () => {
      if (!liffId) {
        document.getElementById('status').textContent = 'ไม่พบค่า LIFF_ID ในระบบ';
        return;
      }
      if (typeof liff === 'undefined') {
        document.getElementById('status').textContent = 'กำลังโหลด LIFF SDK...';
        window.addEventListener('load', launchAdmin, { once: true });
        return;
      }
      launchAdmin();
    });
  </script>
</head>
<body>
  <div class="card">
    <h1>NILA · Admin Launcher</h1>
    <p>ระบบจะเปิดหน้า <strong>Line ERP Admin</strong> ใน Chrome / Safari โดยอัตโนมัติ</p>
    <div class="status" id="status">กำลังสร้างเซสชัน LIFF...</div>
    <a id="fallback-link" class="btn" href="<%- (baseUrl || '') + targetPath %>" target="_blank" rel="noopener" hidden>เปิดด้วยตัวเอง</a>
    <p class="muted">หากไม่ถูกพาออกจาก LINE ให้กดปุ่มด้านบนเพื่อเข้าใช้งาน</p>
  </div>
</body>
</html>
