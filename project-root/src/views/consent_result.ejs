<!-- project-root/src/views/consent_result.ejs -->
<section class="auth-card">
  <div class="logo">
    <img src="/static/img/nila-logo.png" alt="Sorrni" />
    <div class="title">ผลการดำเนินการ</div>
  </div>

  <% if (accepted) { %>
    <h2 class="mt-0">ขอบคุณสำหรับความยินยอม</h2>
    <p class="muted">
      เราได้บันทึกการยินยอมของคุณเรียบร้อยแล้ว คุณสามารถปิดหน้าต่างนี้และกลับไปที่ LINE ได้เลย
    </p>
  <% } else { %>
    <h2 class="mt-0">คุณได้ปฏิเสธการยินยอม</h2>
    <p class="muted">
      เราจะไม่เก็บและประมวลผลข้อมูลการสนทนาของคุณ หากต้องการเปลี่ยนใจ สามารถกดลิงก์ยืนยันอีกครั้งในภายหลังได้
    </p>
  <% } %>

  <div class="note mt-3">
    Timestamp: <strong><%= new Date(consent.updatedAt).toLocaleString('th-TH') %></strong>
  </div>
</section>
